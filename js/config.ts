export class Config {
    static isSortByDate: boolean = false;
    static isSortByRating: boolean = false;
    static isSortByResponses: boolean = false;
    static sortOrder: 'asc' | 'desc' = 'desc';
    
    // Единая переменная для всех классов
    static showFavorites: boolean = false; 
}